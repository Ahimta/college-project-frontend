div.row
  article.col-md-6.text-center(ng-repeat='section in sections')
    header: h2(ng-bind='section.title')

    p(ng-bind='section.body')

    footer: a.btn.btn-primary(role='button' ng-href='{{section.link}}'
      ng-disabled='{{!section.done}}') التفاصيل
