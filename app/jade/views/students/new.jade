extends ../../layouts/dashboard

include ../../mixins/accountable/usernametakenalert
include ../../mixins/conflictalert
include ../../mixins/students/fields

block header
  | إضافة طالب

block content
  section
    +conflict-alert('usernameConflict', 'اسم المستخدم أو الرقم الجامعي موجودة سابقاً')

    form(name='myForm' ng-submit='create()')
      +student-fields('student_account', 'guides', 'usernameConflict', true)

      div.form-group: input.btn.btn-success.btn-block.btn-lg(type='submit'
          value='إضافة مستخدم'
          ng-disabled='myForm.$invalid')
