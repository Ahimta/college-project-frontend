mixin accountables-table(accountables)

  div.table-responsive
    table.table.table-hover.table-condensed.text-center
      thead
        tr
          th.text-center الاسم
          th.text-center اسم المستخدم
      tbody
        tr(ng-repeat=('accountable in ' + accountables + ' | filter:'+ filter))
          td(ng-bind='accountable.full_name')
          td(ng-bind='accountable.username')
